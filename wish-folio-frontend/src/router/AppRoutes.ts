export enum AppRoutesNames {
    HOME = "home",
    LOGIN = "login",
    REGISTER = "register",
}

export const AppRoutesPaths = {
    [AppRoutesNames.HOME]: "/",
    [AppRoutesNames.LOGIN]: "/login",
    [AppRoutesNames.REGISTER]: "/register",
}